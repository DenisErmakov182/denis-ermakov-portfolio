# Хэндофф — работа над кейсами, 04.08.2026

## Правила — читать первым

- **Не коммитить и не пушить** без явного подтверждения Дениса
- Не отправлять сообщения в Telegram, не менять статусы на внешних сервисах
- TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID — никогда не выводить, не запрашивать
- Локальный сервер: уже настроен в `.claude/launch.json` (portfolio, порт 3000)

---

## Стек

- Чистый HTML/CSS/JS, без фреймворков, без сборщика
- Кейсы: `gptest.html`, `coalition.html`, `mappy.html`, `admin.html`
- Стили кейсов встроены в `<style>` внутри каждого файла
- Главная: `index.html` + `styles.css` + `script.js`
- Тема: `data-theme="dark"/"light"` на `<html>`
- Язык: `data-ru` / `data-en` на элементах, `applyLang()` в JS кейсов
- Акцент-цвета: coalition=`--blue #3b82f6`, gptest=`--purple #8e51ff`, mappy=`--pink #ff4575`, admin=`--accent #6366f1`

---

## Что было сделано сегодня (04.08)

### Добавлены блоки «Что узнал» / «Что можно улучшить» в каждый кейс
CSS-класс `.insight-pull` с border-left цветом акцента.

### Добавлены hero-метрики (`.hero-metrics` → `.hero-stat`) в шапку кейсов
- Коалиция: −40% TTP · +18% CSAT · 64% adoption
- ГПтест: 20 активных · 5–6 сессий/нед · 127 ответов в опросе
- Mappy: 20 в тесте · 7–8 активных · 2 раунда тестирования
- Admin: 3/3 гипотезы · MVP · B2B 1 заказчик

### Добавлены финальные metric-pills в тело кейсов
- `gptest.html` — раздел «Результаты»: 20 / 5–6 / 3 итерации
- `mappy.html` — новый раздел «Результаты» перед «Что узнал»: 20 / 7–8 / 4/4

### Метрики на карточках главной (`.card-metrics` → `.card-stat` в `styles.css`)
- ГПтест: 20 активных · 5–6 сессий/нед
- Mappy: 20 участников · 7–8 активных
- Коалиция: −40% TTP · +18% CSAT · 64% adoption
- Admin: 3/3 гипотезы · MVP

### Мелкие правки
- `row-gap` кейс-грид на мобилке: 16px → 32px (в `styles.css` @media 640px)
- Из «Планов» ГПтест удалён пункт «Анимацию загрузки» — уже реализована

---

## Статус git

**Ветка:** `main`
**Не закоммичено:** `coalition.html`, `gptest.html`, `mappy.html`, `admin.html`, `index.html`, `styles.css`, `worker/src/index.js`

---

## Оценка кейсов (итог дня)

| Кейс | Оценка | Главная проблема |
|---|---|---|
| Коалиция | 7/10 | Нет N людей в research, нет гипотез, нет «было» |
| ГПтест | 8/10 | Нет целевых метрик ДО, планы без статуса, нет видео |
| Mappy | 6/10 | Нет N людей, нет целевых метрик, нет планов |
| Admin | 7/10 | Нет точки А (скрин «до»), нет финальных числовых метрик |

---

## Следующие задачи (по приоритету)

### P1 — кейсы
1. **Коалиция Research** — добавить: «Поговорили с 9 преподавателями, задавали вопросы о рабочем процессе создания урока. Тестировали макеты на тех же учителях.» Файл: `coalition.html`, раздел `Research`, `div.case-content > p.content-body`
2. **Mappy Research** — добавить N людей: сколько было в интервью + сколько в опросе. Файл: `mappy.html`, раздел `Research`
3. **Admin Research** — добавить N людей в интервью. Файл: `admin.html`, раздел `Research`

### P2 — кейсы
4. **Admin** — добавить «точку А»: скрин переписки в мессенджерах (как выглядел хаос до). Файл: `admin.html`, раздел `Проблема` или отдельный блок «Было»
5. **ГПтест Планы** — разделить: что уже реализовано vs что следующее. Файл: `gptest.html`, раздел `Планы`
6. **Коалиция** — добавить гипотезы явно (H1: если дать загрузку напрямую... H2: если добавить расшифровку...)

### P3 — медиа
7. Скринкаст ГПтест → Vimeo → iframe в кейс
8. Скринкаст Mappy → Vimeo → iframe в кейс

### Отдельно — главная
9. Добавить уровень «Middle Product Designer» в intro секцию
10. LinkedIn ссылка в шапку

### Отдельно — отклики
- Сбер — Кобзев (не написали)
- VK — Анастасия (не написали)
- Ozon — Дарья (писала 28.07, нужен follow-up)
- Альфа-Банк — Николаева (не написали)
- 5 карточек Getmatch в Obsidian — не созданы (см. старый HANDOFF.md)

---

## Obsidian — что там есть

Папка: `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/Портфолио/`

| Файл | Что внутри |
|---|---|
| `Контекст и состояние работы 2026-08-04.md` | Полный итог дня |
| `Выводы и данные по кейсам — 2026-08-04.md` | Q&A по кейсам, черновики выводов |
| `Оценка кейсов — 2026-08-04.md` | Оценки 6–8/10 с конкретными пробелами |
| `Портфолио и кейсы — рекомендации.md` | Все чеклисты + инсайты канала Яны (84 поста) |

---

## Mappy — продолжение 05.08

### Текущее состояние — реализовано

- В hero Mappy стоят три статичных кадра, а не видео:
  - `assets/mappy/hero-map.png`;
  - `assets/mappy/hero-filters.png` — стоп-кадр из `filters-and-friends.mp4` на второй секунде;
  - `assets/mappy/hero-place-card.png` — стоп-кадр из `place-card.mp4` на второй секунде.
- Метрика в «Результатах» исправлена: `4/4 гипотезы подтверждены` заменена на `2 раунда тестирования`.
- В «Тестировании» две итерации оформлены внутри широкого `img-block`.
  - Итерация 1: заголовок и основной текст о неудобном редактировании названия/адреса, лимите в 4 фото и запросе на подписи к конкретной фотографии.
  - Итерация 1 показывает три отдельных макета в сетке: `iteration-1-empty.png`, `iteration-1-rating.png`, `iteration-1-tags.png`.
  - Итерация 2: текст «Переработал нейминг: адрес и название — унифицированные текстовые поля. Количество фото сократил до четырёх, добавил возможность подписать конкретную фотографию.»
  - В итерации 2 показаны три видео: `place-card.mp4`, `photo-caption.mp4`, `place-card-photo-caption.mp4`.
- У `photo-caption.mp4` отрезаны последние 5 секунд; текущий размер — 9,2 МБ.
- В `assets/mappy/demos/` сейчас используются шесть роликов:
  - `pin-on-map.mp4`;
  - `filters-and-friends.mp4`;
  - `place-card.mp4`;
  - `photo-caption.mp4`;
  - `search.mp4`;
  - `place-card-photo-caption.mp4` — получен из предоставленного файла с рабочего стола.
- Ролики на десктопе запускаются только при наведении и сбрасываются на первый кадр после ухода курсора. На тач-устройствах запускаются автоматически.
- На десктопе под блоками с роликами есть серая плашка: «Наведите на экран, чтобы посмотреть анимацию.» На мобильном она скрыта.
- Раздел «Карта» вынесен перед «Другими экранами» (друзьями) и оформлен так же, как итерации: вводная строка отдельно, далее широкий `img-block` с тремя роликами карты, фильтров и поиска.
- Временно скрыт HTML-комментарием раздел «Решение» с экраном виджета (`screen-4.png`), чтобы позже перенести его в другое место. Ассеты и разметка сохранены.
- Мобильная версия:
  - экраны и ролики внутри кейса идут в одну колонку;
  - скругления визуальных блоков, экранов и роликов — `32px`.
  - На десктопе у hero внешний контейнер — `40px`, внутренние hero-экраны — `20px`.
- `npm run build` и `git diff --check` проходили после последних изменений.

### Важное для следующего продолжения

- Пользователь решил точно добавить сценарий онбординга. Подтверждённый сигнал: статичный экран «Твой пин находится там, где ты» был проигнорирован, пользователь не понял, как продолжить. Готового фикса в истории разработки нет; в кейсе оформлять это как открытый вывод / следующую итерацию, не как реализованное решение.
- Ещё подтверждённые тестовые изменения, которые пока не добавлены отдельным блоком:
  - фильтр «Оценка»: три визуальных состояния пользователь воспринял как баг; во второй версии сделан единый бинарный чип и добавлен вариант `1★`;
  - статусы «Был / Планирую»: независимые чипы заменены на взаимоисключающий селектор.
- Не выдавать за протестированный пользовательский вывод инженерную доработку PWA install-guide: замена растрового скриншота на DOM/SVG — это визуально-техническая итерация.

### Просмотр локально

- Локальный сервер работает на `http://localhost:3000`.
- Браузер может показывать закешированную версию `mappy.html`; для проверки изменений открывать адрес с новым параметром, например `http://localhost:3000/mappy.html?v=новая-метка`.

---

## Mappy — итог 05.08.2026

### Что завершено и опубликовано

- Из Mappy убраны временные блоки «Что изменил по итогам теста», карточки с выводами о фильтрах/статусах и блок «Открытый вывод: онбординг» — пользователь попросил пока не показывать их в кейсе.
- В разделе «Карта» у трёх роликов добавлены подписи в формате «Было / Стало»:
  - **Пин на карте:** без адреса → сверху добавлена пилюля с адресом;
  - **Фильтры и места друзей:** плотные пины → кластеризация при увеличении и слайдер выбора нужного пина;
  - **Поиск:** только сохранённые места → поиск по всем адресам.
- Оформление подписей: названия роликов — `font-weight: 700`; основной текст — серый; только метки «Было:» и «Стало:» — белые, с той же обычной жирностью, что и основной текст (`font-weight: 400`).
- Под каждым роликом реализован собственный прогресс-бар:
  - расположен поверх видео;
  - высота всей пилюли — **8 px**;
  - на десктопе показывается только при наведении на ролик;
  - на тач-устройствах виден постоянно;
  - десктоп: ролик запускается при наведении и сбрасывается после ухода курсора; мобильный: ролики запускаются автоматически, без звука, с `playsinline` и `loop`.
- Проверка перед публикацией пройдена: `npm run build` и `git diff --check` без ошибок.

### Git и публикация

- Создана ветка `codex/mappy-case-media`.
- В коммит попали **только Mappy-файлы**: `mappy.html`, шесть mp4 из `assets/mappy/demos/` и шесть изображений из `assets/mappy/`.
- Коммит: `79d3176 Update Mappy case media`.
- Коммит отправлен напрямую в `origin/main` 05.08.2026. После деплоя GitHub Pages Mappy доступен по стандартному пути репозитория: `https://denisermakov182.github.io/denis-ermakov-portfolio/mappy.html`.
- Локальная рабочая копия остаётся на `codex/mappy-case-media`; это нормально. VS Code может показывать 38 изменений — они не относятся к Mappy-коммиту и не были отправлены.

### Как владельцу смотреть сайт без искажения статистики

- Открывать на телефоне или компьютере ссылку: `https://denisermakov182.github.io/denis-ermakov-portfolio/mappy.html?analytics=owner`.
- Первый заход сохраняет режим владельца в `localStorage`; далее обычные переходы по сайту остаются в этом режиме. События получают источник `owner` и исключаются из статистики просмотров.
- Чтобы снова включить обычный учёт посещений на конкретном устройстве, открыть: `https://denisermakov182.github.io/denis-ermakov-portfolio/mappy.html?analytics=public`.

### Не включено в коммит Mappy

- В рабочей директории остались независимые пользовательские изменения: `analytics.js`, `worker/src/index.js`, `.claude/`, `CLAUDE.md`, `assets/Denisphoto.png`, `assets/staff/`, `output/`, `tmp/`, `worker/HANDOFF.md` и другие перечисленные в `git status` файлы.
- Не добавлять их в следующий коммит без отдельного согласования с Денисом.

---

## Продолжение 05.08 — финальные правки перед передачей в Claude

### Что изменено во всех кейсах

- Из первого экрана всех четырёх кейсов убраны карточки hero-метрик:
  - `gptest.html`;
  - `coalition.html`;
  - `mappy.html`;
  - `admin.html`.
- Цифры, которые уже есть в содержательной части кейсов, сохранены. Шапки стали чище и не создают ощущения неподтверждённых результатов без контекста.

### Mappy — новые сценарии «Другие экраны»

- Статичный экран друзей заменён тремя видео в разделе «Другие экраны»:
  - `assets/mappy/demos/saved-places.mp4` — «Сохранённые места»;
  - `assets/mappy/demos/friend-requests.mp4` — «Уведомления и запросы»;
  - `assets/mappy/demos/friend-search.mp4` — «Поиск друга».
- Их исходники по-прежнему лежат на рабочем столе Дениса: `сохраненное.mov`, `Друзья.mov`, `Друг поиск.mov`.
- Дизайн-система вынесена в отдельный статичный блок ниже. `screen-5.png` остаётся в ассетах, но больше не используется в разметке.
- Подписи сценариев:
  - **Сохранённые места:** базовые карточки → быстрые действия; план — в первые три захода легко покачивать карточку, чтобы подсказать неявную возможность быстрого редактирования.
  - **Уведомления и запросы:** входящие и исходящие запросы были смешаны → добавлен центр уведомлений; в будущем — push-уведомления о новых функциях и запросах друзей.
  - **Поиск друга:** поиск уходил наверх вместе с карточкой друга → карточка прокручивается, а поиск поднимается и фиксируется в выбранной точке.

### Mappy — визуальные и поведенческие правила

- На мобильном изображения и ролики с интерфейсом ограничены шириной `300px`, центрированы и сохраняют полные поиск/навигацию.
- Прогресс-бар находится внутри контейнера ролика, поэтому не выходит за его границы. Высота пилюли — `8px`.
- Видео:
  - на десктопе запускаются по hover и сбрасываются после ухода;
  - на мобильном запускаются, когда около 92% ролика появляется в области просмотра, и сбрасываются при уходе из неё;
  - прогресс-бар виден постоянно на мобильном и только по hover на десктопе.
- Отступы:
  - между текстом секции и связанным с ним блоком в Research, Testing и Map — `16px` на всех брейкпоинтах;
  - в Testing на мобильном от текста итерации до экранов — `24px`;
  - между карточками роликов карты на мобильном — `48px`.

### Проверки и публикация

- Перед коммитом запустить: `npm run build` и `git diff --check`.
- `npm run build` и `git diff --check` прошли перед публикацией.
- Изменения опубликованы в `main` коммитом `350899c Refine case studies and Mappy demos`.
- В коммит вошли только `admin.html`, `coalition.html`, `gptest.html`, `mappy.html` и три новых mp4 из `assets/mappy/demos/`.
- Не добавлять `HANDOFF-CASES-2026-08-04.md` в git без отдельного запроса Дениса: это локальный рабочий документ для следующего чата.
